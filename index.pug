<!DOCTYPE html>
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    meta(http-equiv="X-UA-Compatible", content="ie=edge")
    link(href="style.css", rel="stylesheet")
    title Draw me a cat
  body
    .container
      h2 Draw me a cat
      h3 or whatever you want...
      .card
        .card-title Choose a color
        button(onclick="setColor('#BADA55')") #BADA55
        button(onclick="setColor('#CCCCCC')") Gray
        button(onclick="setColor('#5F4B8B')") Ultra Violet
        button(onclick="setColor('#000000')") Basic Black
      .card
        .hal1
          .card-title Draw me a 
            b#topicPrint
            |&nbspPlease !
          button#startBtn(onclick="randomTopic()") Start to draw
        .half1
          .card-title#countDownNotice Count Down Start!
          #countDownNum 
      canvas#draw(width="400" height="400" style="border:1px solid #000")
      button(onclick="cleanCanvas()") Clean canvas
    script(src="style.js")